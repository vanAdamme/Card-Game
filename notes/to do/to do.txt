make obj_init that does stuff game_controller is currently doing

global.allitems ==> obj_player.inventory

make script: findItem("item name") that finds that item from myItems and returns all its data

make pause/inventory disable buttons